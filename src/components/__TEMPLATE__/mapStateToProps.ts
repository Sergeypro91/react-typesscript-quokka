// Store
// import { State } from 'redux/store';

// Selectors

// Types
import { MapStateToPropsType } from './templateComponentTypes';

// export const mapStateToProps = (state: State): MapStateToPropsType => ({});

export const mapStateToProps = (): MapStateToPropsType => ({});
